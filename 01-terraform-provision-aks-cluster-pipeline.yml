# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  name: 'Default'

# Stage-1: Terraform Validate Stage
## Step-1: Publish Artifacts to Pipeline (Pipeline artifacts provide a way to share files between stages in a pipeline or between different pipelines. )
## Step-2: Install Latest Terraform (0.13.5) (Ideally not needed if we use default Ubuntu Agents)
## Step-3: Validate Terraform Manifests (terraform init, terraform validate)

stages:
- stage: TerraformValidate
  jobs:
  - job: TerraformValidateJob
    continueOnError: false
    steps:
    - task: PublishPipelineArtifact@1
      displayName: Publish Artifacts
      inputs:
        targetPath: '$(System.DefaultWorkingDirectory)/terraform-manifests'
        artifact: 'terraform-manifests-out'
        publishLocation: 'pipeline'

    - task: TerraformInstaller@0
      displayName: Terraform Install
      inputs:
        terraformVersion: 'latest'

    - task: PowerShell@2
      displayName: 'Terraform Init & Validate (Service Principal)'
      inputs:
        targetType: inline
        workingDirectory: '$(System.DefaultWorkingDirectory)/terraform-manifests'
        script: |
          Write-Host "Using Terraform from PATH:"
          terraform --version

          # Configure Terraform to use Service Principal auth
          $env:ARM_CLIENT_ID       = "$(ARM_CLIENT_ID)"
          $env:ARM_CLIENT_SECRET   = "$(ARM_CLIENT_SECRET)"
          $env:ARM_TENANT_ID       = "$(ARM_TENANT_ID)"
          $env:ARM_SUBSCRIPTION_ID = "$(ARM_SUBSCRIPTION_ID)"

          Write-Host "Running terraform init against AzureRM backend..."
          terraform init `
            -backend-config="resource_group_name=terraform-storage-rg" `
            -backend-config="storage_account_name=terraformstateazureaks3" `
            -backend-config="container_name=tfstatefiles" `
            -backend-config="key=aks-base.tfstate"

          Write-Host "Running terraform validate..."
          terraform validate


